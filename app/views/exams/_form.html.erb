<%= simple_form_for @exam, :html => { :multipart => true } do |f| %>

	<%= f.input :nome, :required => true %>
	<%= f.input :anno, :required => true %>
	<%= f.input :professore, :required => true %>
	<%= f.input :professore2 %>
	<%= f.input :sito, :required => true %>
	
	
	
	<%= f.button :submit, value: lab, :required => true, :class => "btn-default" %>
<% end %>