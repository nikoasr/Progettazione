<%= simple_form_for @exam, :html => { :multipart => true } do |f| %>
	<%= f.input :nome %>
	<%= f.input :anno %>
	<%= f.input :professore %>
	<%= f.input :sito %>
	<%= f.button :submit, :class => "btn-default" %>
<% end %>